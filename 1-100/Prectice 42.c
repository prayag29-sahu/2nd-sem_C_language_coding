#include <stdio.h>
#include <conio.h>
//prectice 42
void main()
 {
    double IP;

    printf("Enter the complex number: ");
    scanf("%lf", &IP);

    printf("The real part of the complex number is: %.2lf", IP);

    getch();
    
}